<%= form_for(dashboard) do |f| %>
  <% if dashboard.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dashboard.errors.count, "error") %> prohibited this dashboard from being saved:</h2>

      <ul>
      <% dashboard.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :query %>
    <%= f.text_field :query %>
  </div>

  <div class="field">
    <%= f.label :priorities_json %>
    <%= f.text_area :priorities_json %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
